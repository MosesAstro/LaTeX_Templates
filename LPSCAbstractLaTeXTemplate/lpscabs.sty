% This is a LaTeX 2e Class file for creating LPSC abstracts
% $Id: lpscabs.sty 11 2008-01-31 15:42:07Z rbeyer $

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{lpscabs}[2005/12/20 LPSC Abstract Class]

% Copyright (C) 2005,2007,2008 Ross A. Beyer
% Copyright (C) 2014 Moses P. Milazzo

% This work is licensed under the Creative Commons
% Attribution-Noncommercial-Share Alike 3.0 License. To view a copy
% of this license, visit http://creativecommons.org/licenses/by-nc-sa/3.0/;
% or, (b) send a letter to Creative Commons, 171 2nd Street, Suite
% 300, San Francisco, California, 94105, USA.

% We use \sloppy to suppress word division and permit larger interword
% spacing so that lines are preferentially broken between words.
\sloppy

%%---------------------------- Global Settings ------------------------

%% Page Style to put numbers at the top.
\pagestyle{myheadings}

\setlength{\voffset}{-0.5in}
\setlength{\hoffset}{-0.5in}
\setlength{\topmargin}{0in}         % = \voffset+1in because LaTeX adds \voffset+1in
\setlength{\topskip}{2em}          % This is the spacing above the text.

\setlength{\marginparwidth}{0.5in} % Margin widths

\setlength{\oddsidemargin}{0in}   	% = 1in because LaTeX adds 1in
\setlength{\evensidemargin}{0in}    % = 1in because LaTeX adds 1in

\setlength{\columnsep}{1em} %set the column separation. This can be in any units.

\setlength{\textheight}{9in}
\setlength{\textwidth}{7.5in}

\setlength{\headheight}{0pt} % We're not using running heads anymore, so remove it.
\setlength{\headsep}{0pt}

\setlength{\footskip}{0pt} % Also, we're not using footers on this page. Remove them.

% Alter some LaTeX defaults for better treatment of figures:
% See p.105 of "TeX Unbound" for suggested values.
% See pp. 199-200 of Lamport's "LaTeX" book for details.
%   General parameters, for ALL pages:
\renewcommand{\topfraction}{1}    % max fraction of floats at top
\renewcommand{\bottomfraction}{1} % max fraction of floats at bottom
%   Parameters for TEXT pages (not float pages):
\setcounter{topnumber}{2}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{2}     % 2 may work better
\setcounter{dbltopnumber}{10}    % for 2-column pages
\renewcommand{\dbltopfraction}{1} % fit big float above 2-col. text; we want to be generous here
                                                          % so that the LPSC abstract can hold a wide figures.
\renewcommand{\textfraction}{0}  % allow minimal text w. figs
%   Parameters for FLOAT pages (not text pages):
\renewcommand{\floatpagefraction}{0.9}      % require fuller float pages
% N.B.: floatpagefraction MUST be less than topfraction !!
\renewcommand{\dblfloatpagefraction}{0.99}   % require fuller float pages

% Turn off page numbering.
\pagenumbering{gobble}

% Make the section labels and text start on the same line.
\renewcommand\section{\@startsection {section}{1}{\z@}%
                                   {3.5ex \@plus1ex \@minus.2ex}%
                                   {-1em}%
                                   {\normalfont\Large\bfseries}}
\renewcommand\subsection{\@startsection{subsection}{2}{\z@}%
                                     {3.25ex\@plus1ex \@minus.2ex}%
                                     {-1em}%
                                     {\normalfont\large\bfseries}}
\renewcommand\subsubsection{\@startsection{subsubsection}{3}{\z@}%
                                     {3.25ex\@plus1ex \@minus.2ex}%
                                     {-1em}%
                                     {\normalfont\normalsize\bfseries}}                                   
%%---------------------------- Commands ----------------
%%
%% We need to create some commands that the LPSC template will use

\newcommand{\titlearea}[2]{%
\thispagestyle{empty}
\twocolumn[ {\textbf{\Large#1}} #2 ]
}

% The LPI doesn't recommend using a running title anymore, but this 
% command is left in for historical reasons.
%\newcommand{\runningtitle}[1]{%
%\markboth{#1}{#1}
%}
